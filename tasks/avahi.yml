#
- name: avahi - set host name
  hostname:
    name: "{{ project_name }}"

- name: avahi - install
  apt:
    name: "avahi-daemon"
    state: "present"

- name: avahi - fix "sudo, unable to resolve host"
  lineinfile:
    dest: "/etc/hosts"
    line: "127.0.0.1 {{ project_name }}"
